const page = async ({
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  searchParams,
}: {
  searchParams: {
    slug: string;
  };
}) => {
  return <div>page</div>;
};
export default page;